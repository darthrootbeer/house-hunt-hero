---
id: F0C541
status: pending
deps: [B81B06]
files: [scripts/inspect_platform.py]
---
::context
Create interactive tool for investigating platform pages and finding correct CSS selectors. Helps manually inspect pages to identify listing containers, URL patterns, title locations.

::done-when
- Script accepts platform adapter name as argument
- Navigates to platform URL using Playwright
- Displays page screenshot
- Shows accessibility tree snapshot
- Tests CSS selectors interactively (shows matched elements)
- Displays matched elements with HTML
- Exports findings for adapter update

::steps
1. Create scripts/inspect_platform.py
2. Accept adapter name, load adapter class
3. Navigate to adapter.SEARCH_URL
4. Take screenshot, save to temp file, display path
5. Generate accessibility tree snapshot
6. Interactive selector tester: input CSS selector, show matches
7. Display matched elements with HTML snippet
8. Export findings to text file for reference

::avoid
- Don't modify adapter code automatically (manual review needed)
- Don't run in headless mode by default (show browser window)
- Don't require full test suite to run (standalone tool)

::notes
- Use agent-browser or Playwright with headed=True
- Tool helps identify correct selectors before code changes
- Export findings can be referenced when updating adapters
