---
id: 5B06FE
status: done
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
Nextdoor is neighborhood-based and may have early listings. Requires account/neighborhood access.

::done-when
- Nextdoor adapter implemented
- Source config created
- Registered in registry.py
- Adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector accuracy)

::steps
1. Research Nextdoor API availability
2. Check GitHub for Nextdoor scrapers
3. Test Playwright with Nextdoor (may require login)
4. Research public endpoints or RSS feeds
5. Create nextdoor.py adapter
6. Create source config with: neighborhoods, search_urls, auth_required flag
7. Implement fetch() method
8. Register in registry
9. Test adapter immediately after implementation:
   a. Run adapter.fetch() with actual Maine neighborhoods (Gorham, Brunswick, etc.)
   b. Verify listings are retrieved (check count > 0 if listings exist)
   c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
   d. Verify listing_urls are absolute and valid Nextdoor URLs
   e. Check that selectors correctly extract price, location, description
   f. Test with different Maine neighborhoods
   g. Verify neighborhood filtering works correctly
   h. Document any selector issues, auth problems, or data quality issues
   i. Refine selectors/config if listings are not retrieved correctly
10. Run end-to-end test: verify adapter works in full pipeline

::avoid
- Violating ToS
- Storing user credentials
- Overly complex auth flows

::notes
Nextdoor requires neighborhood verification. May need to use public search if available, or document manual neighborhood setup in config.
