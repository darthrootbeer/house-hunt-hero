---
id: B81B06
status: pending
deps: []
files: [test_all_platforms.py]
---
::context
Add diagnostic capture to test script for platforms returning zero properties. Capture screenshots, HTML snapshots, network requests, console errors to diagnose selector issues.

::done-when
- Test script captures screenshots for zero-property platforms
- Saves HTML snapshots to reports/diagnostics/{adapter_id}/page.html
- Logs network requests and console errors
- Records timing metrics (page load, selector wait times)
- Adds --diagnose flag to enable detailed diagnostics

::steps
1. Add diagnostic capture functions to test_all_platforms.py
2. Create reports/diagnostics/ directory structure
3. Capture screenshot after page load for zero-property platforms
4. Save page HTML to snapshot file
5. Capture network requests (filter for XHR/fetch)
6. Capture console errors/warnings
7. Add --diagnose command-line flag
8. Generate diagnostic report summary

::avoid
- Don't capture diagnostics for working platforms (unless flag enabled)
- Don't store full page HTML for all platforms (only zero-property or --diagnose)
- Don't slow down normal test execution significantly

::notes
- Diagnostics should help identify why selectors aren't matching
- Use Playwright's network/console APIs
- Store diagnostics in reports/diagnostics/{adapter_id}/
