---
id: 9F80BF
status: done
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
National aggregators (Zillow, Realtor.com, Redfin, Trulia, Homes.com) typically aggregate from MLS but may have FSBO or early listings. Lower priority but comprehensive coverage.

::done-when
- All 5 national aggregators have adapters
- Source configs created
- Registered in registry.py
- Each adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector/API accuracy, rate limit compliance)

::steps
1. Research each platform: check for API access, public search URLs
2. Check GitHub for Zillow/Realtor.com/Redfin scrapers (many exist)
3. Test existing libraries vs custom scraping
4. Research rate limits and anti-bot measures
5. Create adapters for each platform
6. Create source configs with: search_urls, listing_selectors, api_endpoints (if available), rate_limits
7. Implement fetch() methods
8. Register in registry
9. Test each adapter immediately after implementation:
   a. Run adapter.fetch() with actual Maine location searches (e.g., "Maine", "Portland ME", "Bangor ME")
   b. Verify listings are retrieved (check count > 0 if listings exist)
   c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
   d. Verify listing_urls are absolute and valid
   e. Check that selectors/API correctly extract property details, price, location, images
   f. Test with different Maine locations and property types
   g. Verify rate limits are respected (check timing between requests)
   h. Monitor for anti-bot blocking and document any issues
   i. Document any selector issues, API problems, or data quality issues
   j. Refine selectors/config/rate limits if listings are not retrieved correctly
10. Run end-to-end test: verify all adapters work in full pipeline

::avoid
- Violating ToS with aggressive scraping
- Paid API access unless necessary
- Over-complicating if public APIs exist

::notes
These sites have sophisticated anti-bot measures. May need Playwright with stealth plugins. Many GitHub projects exist for these - evaluate and adapt. Store rate limits carefully in config.
