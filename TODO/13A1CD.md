---
id: 13A1CD
status: pending
deps: [39D5EA]
files: [tests/test_adapters_with_mock_data.py]
---
::context
Create unit tests that test adapter parsing logic with HTML fixtures. Validates that adapters can extract properties from known HTML structure without needing live sites.

::done-when
- Test file loads HTML fixtures for each adapter
- Creates mock Playwright page object with fixture HTML
- Runs adapter's fetch() method with mock page
- Verifies: properties extracted, correct count, valid URLs/titles
- Tests edge cases (missing fields, malformed HTML)
- Fast execution (no network requests)

::steps
1. Create tests/test_adapters_with_mock_data.py
2. Create mock Playwright page class that serves HTML fixtures
3. For each adapter: load corresponding HTML fixture
4. Run adapter.fetch() with mock page
5. Assert property count matches fixture expectations
6. Validate each property: title, URL, payload structure
7. Test edge cases: missing price, empty title, etc.

::avoid
- Don't make real network requests (use fixtures only)
- Don't test live site connectivity (that's integration test)
- Don't duplicate full adapter test logic (focus on parsing)

::notes
- Enables CI/CD testing without network dependencies
- Validates parsing logic independent of site changes
- Fast feedback loop for adapter changes
