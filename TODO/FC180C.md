---
id: FC180C
status: done
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
Land-specific sites may have properties with structures. 5 platforms: LandSearch, KW Land, The Maine Land Store, LandWatch, Land.com. Lower priority unless looking for land with houses.

::done-when
- All 5 land platforms have adapters
- Source configs created
- Registered in registry.py
- Each adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector accuracy, structure filtering)

::steps
1. Research each platform: check for API, search functionality
2. Check GitHub for land listing scrapers
3. Test scraping tools
4. Create adapters for each platform
5. Create source configs with: search_urls, listing_selectors, filters (structures, houses, not raw land)
6. Implement fetch() methods
7. Register in registry
8. Test each adapter immediately after implementation:
   a. Run adapter.fetch() with actual Maine searches, filtering for properties with structures
   b. Verify listings are retrieved (check count > 0 if listings exist)
   c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
   d. Verify listing_urls are absolute and valid
   e. Check that selectors correctly extract property details, price, location, structure info
   f. Verify structure filter works correctly (excludes raw land without structures)
   g. Test with different Maine locations
   h. Verify property type filtering excludes raw land
   i. Document any selector issues or data quality problems
   j. Refine selectors/config if listings are not retrieved correctly
9. Run end-to-end test: verify all adapters work in full pipeline

::avoid
- Raw land without structures
- Over-complicating filters
- Paid API access

::notes
Focus on land listings that include houses/structures. Filter out raw land. Store structure filters in config.
