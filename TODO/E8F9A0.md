---
id: E8F9A0
status: done
deps: []
files: [PLATFORM_TEST_PLAN.md, tests/test_all_platforms.py, reports/platform_test_report.md]
---
::context
Test all 49 active property search platforms to verify they can find and return actual property listings. Generate a readable report without technical jargon showing which platforms work, which found properties, and which need attention.

::done-when
- Test script created that tests all 49 platforms
- Each platform search executes and collects results
- Report generated in plain language showing:
  - Executive summary with counts
  - Results organized by category
  - Platform details (status, properties found, sample listings)
  - Issues and recommendations
- Report saved to reports/platform_test_report.md

::steps
1. Create test script that imports all adapters from registry
2. For each adapter: run fetch(), collect listings, record timing
3. Validate listings have required fields (title, url, source)
4. Categorize results: Working/Empty/Problem/NeedsAttention
5. Generate markdown report with plain language (no tech jargon)
6. Organize report by platform categories
7. Include sample property titles for platforms with results
8. List issues and recommendations section

::avoid
- Don't use technical terms in report (use "property listings" not "RawListing objects")
- Don't test disabled adapters (MunicipalTaxAssessor, MaineStateCreditUnion, MaineCreditUnionsDirectory)
- Don't fail test if platform returns zero listings (may be normal)
- Don't include implementation details in report

::notes
- Follow PLATFORM_TEST_PLAN.md structure
- Use existing test_all_adapters.py as reference but create new focused test
- Report should be readable by non-technical users
- Test all 49 active platforms from registry.get_adapters()
