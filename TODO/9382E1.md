---
id: 9382E1
status: pending
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
Auction houses list properties weeks/months before auction. 3 platforms: EstateSale.com, GoToAuction.com, Homes.com (auction section).

::done-when
- All 3 auction platforms have adapters
- Source configs created
- Registered in registry.py
- Each adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector accuracy, auction date extraction)

::steps
1. Research each platform: check for API, RSS feeds, search URLs
2. Check GitHub for auction site scrapers
3. Test scraping tools (Playwright likely needed for JS-heavy sites)
4. Create adapters for each platform
5. Create source configs with: search_urls, listing_selectors, auction_date_selectors, location_filters (Maine)
6. Implement fetch() methods
7. Register in registry
8. Test each adapter immediately after implementation:
   a. Run adapter.fetch() with actual Maine location searches
   b. Verify listings are retrieved (check count > 0 if listings exist)
   c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
   d. Verify listing_urls are absolute and valid
   e. Check that selectors correctly extract property details, price, location, auction date
   f. Verify auction dates are extracted and formatted correctly
   g. Test with different Maine locations
   h. Verify location filters work correctly
   i. Document any selector issues or data quality problems
   j. Refine selectors/config if listings are not retrieved correctly
9. Run end-to-end test: verify all adapters work in full pipeline

::avoid
- Paid API access
- Login requirements
- Over-complicating date parsing

::notes
Auction sites often have dynamic content. Playwright likely required. Store Maine location filters in config. Extract auction dates for filtering.
