---
id: 945330
status: done
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
Facebook Marketplace and local Facebook groups are high-value early-market sources. Requires Facebook Graph API or scraping.

::done-when
- Facebook Marketplace adapter implemented
- Local Maine real estate groups adapter implemented
- Source configs created
- Registered in registry.py
- Each adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector accuracy)

::steps
1. Research Facebook Graph API for Marketplace listings (check if public access available)
2. Research scraping Facebook Marketplace - check GitHub for solutions
3. Test Playwright with Facebook (may need login simulation or public endpoints)
4. Research Facebook Groups API or scraping methods
5. Create facebook_marketplace.py adapter
6. Create facebook_groups.py adapter (or combine)
7. Create source configs with: api_endpoints, group_ids, search_params, auth_method
8. Implement fetch() methods
9. Register in registry
10. Test each adapter immediately after implementation:
    a. Run adapter.fetch() with actual Maine location searches (e.g., "Maine", "Portland ME", "Bangor ME")
    b. Verify listings are retrieved (check count > 0 if listings exist)
    c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
    d. Verify listing_urls are absolute and valid Facebook URLs
    e. Check that selectors correctly extract price, location, description, images
    f. Test with different Maine locations and property types
    g. Verify group IDs work correctly for groups adapter
    h. Document any selector issues, rate limiting, or data quality problems
    i. Refine selectors/config if listings are not retrieved correctly
11. Run end-to-end test: verify all adapters work in full pipeline

::avoid
- Violating Facebook ToS
- Requiring user login credentials
- Rate limiting that triggers bans

::notes
Facebook aggressively blocks scrapers. May need Graph API access or sophisticated Playwright setup. Groups require joining - may need manual group ID list in config.
