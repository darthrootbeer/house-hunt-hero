---
id: 6EDE07
status: pending
deps: [B81B06]
files: [scripts/run_diagnostics.py]
---
::context
Create batch diagnostic runner for multiple platforms. Generates diagnostic reports (screenshots, HTML, network logs) for platforms returning zero properties.

::done-when
- Script accepts platform list or "all-empty" flag
- Runs enhanced diagnostics on specified platforms
- Generates diagnostic reports in reports/diagnostics/{adapter_id}/
- Creates summary report of findings
- Flags common issues: anti-bot protection, selector mismatch, AJAX loading

::steps
1. Create scripts/run_diagnostics.py
2. Accept --platforms flag (list) or --all-empty flag
3. Load list of platforms returning zero properties from test report
4. Run diagnostic capture for each platform
5. Generate individual reports in reports/diagnostics/{adapter_id}/
6. Create summary report: common issues, selector analysis
7. Flag platforms that need manual inspection vs automatic fix

::avoid
- Don't run diagnostics on all platforms (too slow)
- Don't duplicate test_all_platforms.py logic (reuse diagnostic code)
- Don't run in parallel (may trigger rate limits)

::notes
- Helps batch-process all zero-property platforms
- Summary report identifies patterns across platforms
- Speeds up diagnosis phase before fixing
