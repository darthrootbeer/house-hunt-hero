---
id: 45DD1A
status: pending
deps: [B81B06]
files: [tests/test_selector_validation.py]
---
::context
Create selector validation test that checks if CSS selectors actually match elements on live pages. This helps identify broken selectors before running full adapter tests.

::done-when
- Test file created that navigates to platform URLs
- Tests each selector in adapters against live pages
- Reports: count of elements found, sample element HTML
- Flags selectors that find zero elements
- Compares working vs non-working adapter selector strategies

::steps
1. Create tests/test_selector_validation.py
2. Extract selectors from adapter code (query_selector_all calls)
3. Navigate to each platform's SEARCH_URL
4. Test each selector, count matching elements
5. Extract sample HTML from matched elements
6. Generate report: selector -> element count -> sample HTML
7. Compare patterns between working and non-working adapters

::avoid
- Don't test all platforms in single run (too slow)
- Don't test selectors for working platforms unless needed
- Don't make actual property extraction - just selector validation

::notes
- Focus on platforms returning zero properties
- Use Playwright to test selectors
- Report helps identify which selectors need updating
