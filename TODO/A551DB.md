---
id: A551DB
status: pending
deps: [706A0E]
files: [src/ingestion/adapters/oodle.py, src/ingestion/adapters/town_ads.py, src/ingestion/adapters/sun_journal.py, src/ingestion/adapters/advertiser_democrat.py, src/ingestion/adapters/midcoast_villager.py, src/ingestion/adapters/portland_press_herald.py]
---
::context
Fix classifieds & local news platforms returning zero properties. These may have simple HTML structures or require different search parameters. Platforms: oodle, town_ads, sun_journal, advertiser_democrat, midcoast_villager, portland_press_herald.

::done-when
- At least 4 of 6 classifieds platforms finding properties
- Correct selectors identified via page inspection
- Adapter code updated with specific selectors
- Each working platform tested and verified
- Platforms with no listings documented as legitimately empty

::steps
1. Run diagnostics on each classifieds platform
2. Use inspect_platform.py to inspect page structure
3. Identify correct selectors (may be simple HTML structure)
4. Check if different search parameters needed (location filters, etc.)
5. Update adapter code with site-specific selectors
6. Test each adapter independently
7. Verify properties found and data quality
8. Document which platforms have listings vs legitimately empty

::avoid
- Don't assume simple HTML means simple selectors (verify)
- Don't skip checking if search filters need adjustment
- Don't mark as "working" if legitimately no listings available

::notes
- Classifieds sites may have simple HTML (easier to fix)
- Some may require location-specific search parameters
- Local news sites may have limited real estate listings
