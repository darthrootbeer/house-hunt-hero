---
id: D1B0F7
status: done
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
Government tax foreclosure listings from Maine Revenue Services (Unorganized Territory), county probate courts, municipal tax assessor offices. As of Aug 2024, municipalities must list with licensed agents/brokers.

::done-when
- Maine Revenue Services adapter implemented
- County/municipal listing adapters implemented (where public)
- Source configs created
- Registered in registry.py
- Each adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector accuracy, PDF handling if applicable)

::steps
1. Research Maine Revenue Services website for tax-acquired property listings
2. Research county probate court websites for public notices
3. Research municipal tax assessor websites
4. Check GitHub for government property scrapers
5. Test scraping tools (may be PDFs, public notices, or web listings)
6. Create maine_revenue_services.py adapter
7. Create county_probate.py adapter (or per-county if different)
8. Create municipal_tax.py adapter
9. Create source configs with: listing_urls, notice_selectors, pdf_handling, county_codes
10. Implement fetch() methods
11. Register in registry
12. Test each adapter immediately after implementation:
    a. Run adapter.fetch() with actual searches
    b. Verify listings are retrieved (check count > 0 if listings exist)
    c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
    d. Verify listing_urls are absolute and valid
    e. Check that selectors correctly extract property details, location, tax info
    f. If PDFs are involved, verify PDF text extraction works correctly
    g. Test with different counties/municipalities
    h. Verify county code filtering works correctly
    i. Document any selector issues, PDF parsing problems, or data quality issues
    j. Refine selectors/config if listings are not retrieved correctly
13. Run end-to-end test: verify all adapters work in full pipeline

::avoid
- Requiring manual PDF parsing if avoidable
- Over-complicating for simple public notice pages
- Accessing non-public government systems

::notes
Some listings may be PDFs requiring extraction. Some may be simple HTML public notices. Store county/municipality codes in config. May need to check multiple county websites.
