---
id: 5BEC96
status: pending
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
Maine MLS services may have early listings before syndication. 2 platforms: Maine Listings (mainelistings.com - official MLS), Maine State MLS (mainestatemls.com).

::done-when
- Both MLS platforms have adapters
- Source configs created
- Registered in registry.py
- Each adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector/API accuracy)

::steps
1. Research MLS API access (RETS, IDX, or public portals)
2. Check GitHub for MLS/RETS scrapers
3. Test public search portals if available
4. Research RETS protocol if needed
5. Create adapters for each MLS
6. Create source configs with: api_endpoints, rets_config (if applicable), search_params, auth_method
7. Implement fetch() methods
8. Register in registry
9. Test each adapter immediately after implementation:
   a. Run adapter.fetch() with actual Maine property searches
   b. Verify listings are retrieved (check count > 0 if listings exist)
   c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
   d. Verify listing_urls are absolute and valid
   e. Check that API/selectors correctly extract property details, price, location, MLS data
   f. Test with different search parameters (location, price range, property type)
   g. Verify RETS/IDX data parsing works correctly if applicable
   h. Document any API/auth issues or data quality problems
   i. Refine config/implementation if listings are not retrieved correctly
10. Run end-to-end test: verify all adapters work in full pipeline

::avoid
- Paid MLS access fees
- Requiring realtor credentials
- Over-complicating if public portals exist

::notes
MLS systems often use RETS protocol or have public IDX portals. Some may require realtor access. Document access method in config. May need to use public search portals.
