---
id: 7BD1E6
status: pending
deps: [164AEC]
files: [src/ingestion/adapters/maine_state_mls.py]
---
::context
Fix maine_state_mls platform returning zero properties. Inspect live page, identify correct selectors, update adapter code, test fix. Compare with working maine_listings adapter for similar patterns.

::done-when
- maine_state_mls platform finding properties (or documented why not possible)
- Correct selectors identified via page inspection
- Adapter code updated with specific selectors
- Tested and verified working
- Fix documented with selector comparison to maine_listings

::steps
1. Run diagnostics on maine_state_mls
2. Use inspect_platform.py to inspect live page
3. Compare page structure with maine_listings (which works)
4. Identify correct selector pattern (likely URL-based like maine_listings)
5. Update adapter code with correct selectors
6. Add wait conditions if needed (Cloudflare, dynamic loading)
7. Test adapter: verify properties found
8. Document fix pattern

::avoid
- Don't assume same selectors as maine_listings (may differ)
- Don't skip Cloudflare protection handling if present
- Don't forget to test fix independently

::notes
- MLS services may have similar structure
- May need Cloudflare bypass (like maine_listings uses)
- High priority (MLS is authoritative source)
