---
id: 6929E3
status: done
deps: []
files: [src/ingestion/adapters/, configs/sources/, src/ingestion/registry.py]
---
::context
FSBO platforms are highest priority - most likely to have pre-Zillow listings. 8 platforms: Ownerama, Brokerless, Flat Fee Group, The Rock Foundation, FSBOHomeListings.com, DIY Flat Fee MLS, ISoldMyHouse.com, Hoang Realty FSBO Program.

::done-when
- All 8 FSBO platforms have adapters implemented
- Each has source config in configs/sources/ with scraping/search method
- All registered in registry.py
- Each adapter tested with actual search data and verified to retrieve listings successfully
- Test results documented (listings found, data quality, selector accuracy)

::steps
1. Research each platform: check for API, RSS feeds, public search URLs
2. Search GitHub for existing scrapers/libraries for each platform
3. Test Playwright, requests+BeautifulSoup, or other tools per platform
4. Create adapter class for each platform following IngestionAdapter pattern
5. Create source config YAML in configs/sources/ with: url, selectors, method (playwright/html/rss/api), rate_limits
6. Implement fetch() method using best tool for that platform
7. Register in registry.py
8. Test each adapter immediately after implementation:
   a. Run adapter.fetch() with actual Maine search queries
   b. Verify listings are retrieved (check count > 0 if listings exist)
   c. Inspect RawListing objects: verify source_id, listing_url, title, raw_payload populated
   d. Verify listing_urls are absolute and valid
   e. Check that selectors correctly extract price, location, and other data
   f. Test with different search parameters if applicable
   g. Document any selector issues or data quality problems
   h. Refine selectors/config if listings are not retrieved correctly
9. Run end-to-end test: verify all adapters work in full pipeline

::avoid
- Paid API access or credentials
- Login-required sources
- Over-engineering - use simplest working method

::notes
Many FSBO platforms syndicate to Zillow/Realtor.com, but may have earlier listings. Config should store: search_url, listing_selectors, detail_page_selectors, pagination_method, anti-bot_measures.
